# Nix flake outputs
result
result-*
.direnv

# Nix flake lock file is optional - include it for reproducibility
# flake.lock

# Beads runtime files (database, locks, logs)
.beads/*.db
.beads/*.db-shm
.beads/*.db-wal
.beads/*.lock
.beads/*.pid
.beads/*.log
.beads/.local_version
.beads/last-touched
.beads/sync_base.jsonl
*.o
*.a
engine/hexen2/glhexen2
engine/hexen2/h2ded

# CMake build directories
engine/build/
engine/build-*/
CMakeCache.txt
CMakeFiles/

# bv (beads viewer) local config and caches
.bv/
all_output.txt
tree.txt
